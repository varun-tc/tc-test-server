{
  "name": "tc-test-grpc-server",
  "version": "1.0.0",
  "description": "Self-hosted gRPC HelloService, replacing grpcb.in for TC Automation gRPC tests. Deployed as its own Render service since gRPC (HTTP/2) needs a dedicated port, separate from the main tc-test Express/WebSocket service.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.11.1",
    "@grpc/proto-loader": "^0.7.13"
  }
}
